package common.base.listeners
{
	import common.base.interfaces.IListener;

	/**
	 * 监听器基类
	 * 
	 * 所有 监听器都必须继承这个类
	 * @author noah
	 */	
	public class BaseListener implements IListener
	{
		public function BaseListener()
		{
			
		}
	}
}